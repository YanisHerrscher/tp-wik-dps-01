services:
  api:
    image: api2
    deploy:
      mode: replicated
      replicas: 4

  nginx:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api
    ports:
      - "8080:80"